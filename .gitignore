# Ensure gitignore itself prevents accidental overrides
!.gitignore

# Bazel (except our bazel-cache module and docs)
bazel-*
!tofu/modules/bazel-cache/
!docs/research/bazel-*
!docs/build-system/bazel-*
user.bazelrc

# Nix
result
result-*
.direnv/

# Terraform/OpenTofu
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
!beehive.tfvars
!rigel.tfvars
.terraform/
.terraform.lock.hcl
crash.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc
*.tfplan
tfplan
plan.json
plan-output.txt
graph.svg

# Backend config (user-specific)
backend.local.hcl

# Secrets
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx
secrets/

# Organization config (Bates-specific, not upstream)
config/organization.yaml

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Build artifacts
*.tar.gz
*.zip

# Node.js / SvelteKit
node_modules/
app/build/
app/.svelte-kit/
pnpm-debug.log*

# Logs
*.log

# Prevent coauthoring metadata
.git-coauthors
CODEOWNERS

# Local kubeconfig (never commit)
kubeconfig*
*.kubeconfig
.kube/

# Attic local config
.attic/

# Git hooks (local, not committed upstream)
.githooks/

# Claude Code cache keys (public, not secret)
public-key.txt

# Claude AI assistant files (never commit)
.claude/
CLAUDE.md
CLAUDE.txt
claude.md
*.claude.*

# Credentials and tokens
credentials.json
**/credentials/**
token.json
*token*.json
*.credentials

# Local development overrides
local.tfvars
*.local.tfvars
